navigator.appCodeName  현재 브라우저의 코드명을 반환합니다. 현 시점의 모든 브라우저는 "Mozilla"를 반환함

navigator.appName   현재 브라우저의 이름을 반환함. 현 시점의 모든 브라우저는 "Netscape"를 반환

navigator.appVersion  현재 브라우저의 버전 정보를 반환함. 현 시점의 모든 브라우저는 "5.0(win-dows)"를 반환함

navigator.language 현재 브라우저가 사용하고 있는 언어를 반환함. 한국어를 사용하고 있으면 "ko"를 반환함

navigator.product  현재 브라우저의 엔진 이름을 반환함. 크롬의 경우는 "Gecko"를 반환

navigator.plafform 현재 컴퓨터의 운영체제 정보를 제공합니다. 운영체제가 윈도우이고, 시스템 종류가 64비트라도 브라우저가 32비트로 설치되었다면
                    "Win32"라고 나타냄

navigator.onLine    온라인 상태 여부에 대한 정보를 제공합니다. 만일 인터넷이 정상적으로 연결되어있는 상태라면 true를 반환

navigator.userAgent  브라우저와 운영체제의 종합 정보를 제공합니다.
